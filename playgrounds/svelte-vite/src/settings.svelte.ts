// module-level $state shared by components: listed under the "module" pseudo component
export const settings = $state({
  theme: 'light' as 'light' | 'dark',
  fontSize: 16,
  showSettings: true,
})
